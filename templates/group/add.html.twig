{% extends 'base.html.twig' %}

{% block body %}

    {{ form_start(groupForm) }}

    {{ form_rest(groupForm) }}

    <input type="submit" class="button">
    {{ form_end(groupForm) }}

{% endblock %}

{% block title %}

{% endblock %}

